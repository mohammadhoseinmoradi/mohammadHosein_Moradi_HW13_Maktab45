// -----------------------------------------------------------
// ---------- requires ---------------------------------------
const express = require('express');
const app = express();
// -----------urls questions ---------------------------------
// -----------------------------------------------------------
app.get('/', (req, res, next) => {
    res.send("Hello Word !!!")
    next();
})

// ------------ listen port ---------------------------------
// ----------------------------------------------------------
app.listen(3000, () => {
    console.log(" The Server Is Starting 5005 !!!")
})